# Results

This directory contains benchmark results from CI runs.

Files:
- `benchmark_latest.json` - Most recent benchmark run
- `comparison_tenpy.json` - TeNPy comparison (when available)

These files are generated by `python reproduce.py --save` and uploaded as CI artifacts.
