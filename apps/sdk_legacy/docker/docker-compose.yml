version: "3.9"

services:
  # =============================================================================
  # ONTIC_ENGINE BASE
  # =============================================================================
  ontic:
    build:
      context: ../..
      dockerfile: sdk/docker/Dockerfile
      target: base
    image: tigantic/ontic:latest
    container_name: ontic
    volumes:
      - ontic-data:/home/physics_os/data
    restart: unless-stopped

  # =============================================================================
  # ONTIC_ENGINE GPU
  # =============================================================================
  ontic-gpu:
    build:
      context: ../..
      dockerfile: sdk/docker/Dockerfile
      target: cuda
    image: tigantic/ontic:cuda
    container_name: ontic-gpu
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    volumes:
      - ontic-data:/home/physics_os/data
    restart: unless-stopped

  # =============================================================================
  # JUPYTER NOTEBOOK
  # =============================================================================
  jupyter:
    build:
      context: ../..
      dockerfile: sdk/docker/Dockerfile
      target: jupyter
    image: tigantic/ontic:jupyter
    container_name: ontic-jupyter
    ports:
      - "8888:8888"
    volumes:
      - ontic-notebooks:/home/physics_os/notebooks
      - ontic-data:/home/physics_os/data
    restart: unless-stopped

  # =============================================================================
  # API SERVER
  # =============================================================================
  server:
    build:
      context: ../..
      dockerfile: sdk/docker/Dockerfile
      target: server
    image: tigantic/ontic:server
    container_name: ontic-server
    ports:
      - "8000:8000"  # REST API
      - "5555:5555"  # ZMQ Bridge
    volumes:
      - ontic-data:/home/physics_os/data
    environment:
      - ONTIC_ENGINE_LOG_LEVEL=INFO
      - ONTIC_ENGINE_MAX_WORKERS=4
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    restart: unless-stopped

  # =============================================================================
  # PROMETHEUS METRICS
  # =============================================================================
  prometheus:
    image: prom/prometheus:latest
    container_name: ontic-prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus-data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
    restart: unless-stopped

  # =============================================================================
  # GRAFANA DASHBOARD
  # =============================================================================
  grafana:
    image: grafana/grafana:latest
    container_name: ontic-grafana
    ports:
      - "3000:3000"
    volumes:
      - grafana-data:/var/lib/grafana
      - ./grafana/provisioning:/etc/grafana/provisioning:ro
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_USERS_ALLOW_SIGN_UP=false
    depends_on:
      - prometheus
    restart: unless-stopped

volumes:
  ontic-data:
  ontic-notebooks:
  prometheus-data:
  grafana-data:

networks:
  default:
    name: ontic-network
